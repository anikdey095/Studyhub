import Hero from '@/components/dashboard/Hero';

const HomePage = () => {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center bg-[#0a0a0f] p-8">
      <Hero />
    </main>
  );
};

export default HomePage;